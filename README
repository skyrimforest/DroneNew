主要内容总结
AI 部分已完成调试，需将其集成到界面中。

信号监测与存储逻辑：
当曹师兄监测到某频段连续 3 次出现信号时，触发存储操作。
存储内容为一段时长 100ms 的信号数据，数据量至少包含 3000000 个 float32 格式的点，以文件形式保存。

信号类型识别流程：将存储的信号文件输入已调试好的网络模型，即可返回信号类型。
数据集说明：原数据集包含 5 种信号类型，但因文件过大无法传输，目前仅提供 3 种类型的数据集，相关信息已写入 README 文件。

